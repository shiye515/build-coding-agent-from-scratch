{
  "name": "tcode",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "bin": {
    "tcode": "./dist/index.js"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "test": "node --import tsx --test test/*.test.ts",
    "install:global": "tsc && pnpm install -g --force ."
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "typescript": "^5.0.0"
  }
}
